<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="be.niko.nikoton">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NikoTon"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".laatsteTest.AuthrecActivity"
            android:exported="false"
            android:label="@string/title_activity_authrec"
            android:theme="@style/Theme.NikoTon.NoActionBar" />
        <activity
            android:name=".laatsteTest.ImportActivity"
            android:exported="false" />
        <activity
            android:name=".laatsteTest.PracticeActivity"
            android:exported="false"
            android:label="@string/title_activity_practice"
            android:theme="@style/Theme.NikoTon.NoActionBar" />
        <activity
            android:name=".laatsteTest.MainLTActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".laatsteTest.LT_android"
            android:exported="true">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>